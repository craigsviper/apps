/// <reference types="vite/client" />

// Injected at build time by vite.config.ts `define` from package.json's
// version field — see App.tsx sidebar footer and Backup.tsx for usage.
declare const __APP_VERSION__: string;
